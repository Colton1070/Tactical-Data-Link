// AG0_TDLSelectorLine — superseded by SCR_SpinBoxComponent in v1.5 of the
// marker tool panel. Dynamic-text selector lines didn't survive gamepad
// focus testing (the inner TextWidget wasn't focusable), so we swapped to
// vanilla SCR_SpinBoxComponents which have built-in d-pad cycling, focus
// handling, and arrow visuals. This file is now empty; safe to delete
// the .c + .meta pair from your project.
